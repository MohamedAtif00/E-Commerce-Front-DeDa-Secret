import { ImageUploaderDirectiveDirective } from './image-uploader-directive.directive';

describe('ImageUploaderDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageUploaderDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
